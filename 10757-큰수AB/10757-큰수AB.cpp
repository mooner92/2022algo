#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main(){
    string a,b;
    vector <int> v1;
    getline(cin,a);
    getline(cin,b);
    int sizea = sizeof(a)/sizeof(int);  // == a.size()
    int sizeb = sizeof(b)/sizeof(int);
}

