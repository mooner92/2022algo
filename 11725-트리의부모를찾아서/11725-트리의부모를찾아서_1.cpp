#include <iostream>
#include <vector>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    // vector<int> v;
    int ds[100001] = {0};
    int n, a, b;
    for (int i = 0; i < n - 1; i++)
    {
        cin >> a >> b;
    }
}