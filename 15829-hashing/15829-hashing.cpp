#include <iostream>
#include <string>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int a,sum=0;
    string s;
    cin>>a;
    cin>>s;
    for(int i=0;i<a;i++){

    }

    

    
}
